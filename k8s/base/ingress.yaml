apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: s3-monitor
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: s3-monitor
            port: 
              number: 8000
